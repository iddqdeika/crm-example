# qualityboard Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-26

## Active Technologies
- TypeScript ~5.6.2, React 18.3.1, Python >=3.12 (backend — unchanged) + Vite 5.4.10, React Router DOM 6.28.0 (002-ui-design-landing)
- N/A (no data persistence changes) (002-ui-design-landing)
- TypeScript ~5.6.2, React 18.3.1 + Vite 5.4.10, React Router DOM 6.28.0 (003-fix-ui-design-pages)
- Python 3.12 (backend), TypeScript / React 18 (frontend — no changes) + FastAPI, SQLAlchemy async, `boto3` (S3 client), MinIO Docker image (004-media-persistence)
- PostgreSQL (metadata), MinIO/S3 (binary objects) (004-media-persistence)
- Python 3.12 (backend), TypeScript / React 18 (frontend) + FastAPI, SQLAlchemy async, `redis[asyncio]>=5.0` (new), Alembic, React Router v6 (005-session-persistence)
- PostgreSQL (authoritative session records), Redis (session cache — hot path) (005-session-persistence)
- Python 3.12 (backend), TypeScript / React 18 (frontend) + FastAPI, SQLAlchemy async, React Router v6, existing auth and media (MinIO) stack (006-campaign-management)
- PostgreSQL (campaigns, ad groups, creatives, column config); existing MinIO for creative icon/image blobs (006-campaign-management)
- Python 3.13 (backend), TypeScript (frontend) + FastAPI, React, Vite, Nginx (007-separate-api-routes)
- PostgreSQL (unchanged) (007-separate-api-routes)
- TypeScript (Playwright Test framework) + `@playwright/test` (already installed in `frontend/package.json`) (008-playwright-ui-tests)
- N/A — tests operate against the running application's PostgreSQL via the UI (008-playwright-ui-tests)
- Python 3.11 (backend) · TypeScript 5 / React 18 (frontend) + FastAPI + SQLAlchemy (backend) · React Router + Vitest + Playwright (frontend/test) (010-creation-ad-groups)
- PostgreSQL (dev/prod) · SQLite in-memory (backend tests) (010-creation-ad-groups)
- Python 3.13 (backend), TypeScript / React 18 (frontend) + FastAPI 0.115, SQLAlchemy 2.0 async, Alembic, TipTap (React WYSIWYG editor), `meilisearch` Python client (011-blog-section)
- PostgreSQL 16 (primary), MinIO (image uploads), Meilisearch v1.x (search index) (011-blog-section)
- Python 3.12+ (backend), TypeScript / React 18 (frontend) + FastAPI, SQLAlchemy 2.0 async, Alembic, React Router, Meilisearch Python client (existing) (012-blog-seo-slugs)
- PostgreSQL (blog_posts extended, slug_history table), Meilisearch (index documents include slug; search/listing filter by status) (012-blog-seo-slugs)
- Python 3.12+ (backend), TypeScript / React 18 (frontend) + FastAPI, React Router, existing AuthContext and auth API (013-blog-auth-links)
- None (uses existing sessionStorage `redirectAfterLogin`; no backend change) (013-blog-auth-links)
- Python 3.12+ (backend), TypeScript / React 18 (frontend) + React Router, existing AuthContext; FastAPI backend unchanged (014-landing-auth-blog-nav)
- None (no new persistence; existing auth and blog APIs unchanged) (014-landing-auth-blog-nav)

- Backend: Python 3.12 (FastAPI); Frontend: TypeScript + React + FastAPI, SQLAlchemy, Alembic, Uvicorn, PostgreSQL, React, React Router, React Testing Library (001-adtech-auth-profile)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Backend: Python 3.12 (FastAPI); Frontend: TypeScript + React: Follow standard conventions

## Recent Changes
- 014-landing-auth-blog-nav: Added Python 3.12+ (backend), TypeScript / React 18 (frontend) + React Router, existing AuthContext; FastAPI backend unchanged
- 013-blog-auth-links: Added Python 3.12+ (backend), TypeScript / React 18 (frontend) + FastAPI, React Router, existing AuthContext and auth API
- 012-blog-seo-slugs: Added Python 3.12+ (backend), TypeScript / React 18 (frontend) + FastAPI, SQLAlchemy 2.0 async, Alembic, React Router, Meilisearch Python client (existing)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
